@model SoftUniClone.ServiceModels.Admin.BindingModels.CourseCreationBindingModel
@{
    ViewData["Title"] = "Create";
}

<h2>Create course</h2>

<form asp-area="Admin" asp-controller="Courses" asp-action="Create" method="post" class="form-horizontal">

    @Html.FormGroupFor(model => model.Name)
    @Html.FormGroupFor(model => model.Slug)
    @*<div class="form-group row">
            <label asp-for="Name" class="col-sm-2 col-form-label col-sm-push-1"></label>
            <div class="col-sm-10">
                <input asp-for="Name" class="form-control">
                <span asp-validation-for="Name" class="text-danger"></span>
            </div>
        </div>
        <div class="form-group row">
            <label asp-for="Slug" class="col-sm-2 col-form-label col-sm-push-1">Slug</label>
            <div class="col-sm-10">
                <input asp-for="Slug" class="form-control">
                <span asp-validation-for="Slug" class="text-danger"></span>
            </div>
        </div>*@
    <div class="form-group row">
        <div class="col-sm-10 col-sm-push-2">
            @*<button type="submit" class="btn btn-primary">Sign in</button>*@
            <input type="submit" value="Create" class="btn btn-success" />
            <a asp-area="Admin" asp-controller="Courses" asp-action="Index" class="btn btn-default">Cancel</a>
        </div>
    </div>      
</form>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />

    <script src="~/js/create-slug.js"></script>
        <script>
            $(function () {
                createSlug("@Html.IdFor(model => model.Name)", "@Html.IdFor(model => model.Slug)")
            });
        </script>        
    @* or <script>
            $(function () {
                $("#Name")
                    .change(function (e) {
                        let value = $(this).val();
                        let slug = value.toLowerCase().replace(" ", "-");
                        $("#Slug").val(slug);
                    });
            });
        </script>*@
}