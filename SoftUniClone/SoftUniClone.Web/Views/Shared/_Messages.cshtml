@using SoftUniClone.Web.Extensions

@if (TempData.ContainsKey("__Message"))
{
    var message = TempData.Get<MessageModel>("__Message");
    <message type="@message.Type" message="@message.Message"></message>
}